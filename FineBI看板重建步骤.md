# FineBI 6.0 看板重建操作手册

> 适用数据源：MySQL 连接「制造数仓」→ `ads_db` 的 6 张 ADS 表（**已修正，数据全部正确**）
> 重建原因：原看板的组件把"逐日快照表"当成"单行汇总表"来做**求和**，
> 导致"当日总营收"显示 226 亿（272 天累计）、"利润率"显示 6412%（272 天累计）。
> 本手册按"**表粒度决定聚合方式**"的原则重做，从根上避免这个问题。

---

## 第 0 步：先记住一条铁律（本次翻车的根因）

**同样是"求和"，用在明细表上是对的，用在天级快照表上就是错的。**

| 表 | 粒度（每行代表什么） | 行数 | KPI 卡正确做法 |
|---|---|---|---|
| `ads_boss_dashboard` | **一天**的经营全貌 | 272 | ⚠️ 必须**筛选到某一天**（或取平均值），**不能直接求和** |
| `ads_sale_analysis` | 一天 × 一客户 × 一产品 | 852,817 | ✅ 求和（金额、订单数） |
| `ads_produce_monitor` | 一天 × 一车间 × 一产品 | 255,843 | ✅ 求和（数量）；比率要**重算**，见下 |
| `ads_stock_health` | 一物料 × 一仓库（**单日快照**） | 600 | ✅ 求和（库存金额/数量） |
| `ads_cost_profit` | 一月 × 一产品 × 一车间 | 10,854 | ✅ 求和（成本额）；比率用**平均值** |
| `ads_alert_warning` | **一条预警** | 51 | ✅ 计数（COUNT） |

**比率型字段（%、天数）的正确聚合——不要用"求和"，也别直接"平均值"**，而是用计算字段重算：

| 指标 | 正确公式（FineBI 计算字段） |
|---|---|
| 产能达成率 | `SUM(actual_qty) / SUM(plan_qty)` |
| 良品率 | `SUM(qualified_qty) / SUM(actual_qty)` |
| 交付及时率 | `SUM(order_cnt * delivery_ontime_rate) / SUM(order_cnt)` |
| 毛利率（明细表内） | 先按行算，再 `AVG(profit_rate)` |

---

## 第 1 步：准备数据（10 分钟）

1. 打开 FineBI → `http://localhost:37799`
2. 左侧进入「**数据**」→ 「**分析主题**」
3. 新建主题，命名：`制造数仓看板`
4. 在主题里添加 6 个数据集：数据连接选「**制造数仓**」→ 库 `ads_db` → 依次添加：
   `ads_boss_dashboard`、`ads_sale_analysis`、`ads_produce_monitor`、
   `ads_stock_health`、`ads_cost_profit`、`ads_alert_warning`
5. **数据模式选择**：
   - 推荐「**抽取数据**」（查询快、加载快）
   - 代价：数据变了要手动「更新」（见第 4 步）
   - 如果你希望改完数据库刷新页面就能看到，选「**直连数据**」，但大表（销售分析 85 万行）首次加载会慢
6. 逐个数据集检查字段类型：
   - `stat_date` / `alert_date` → **日期**
   - `stat_month` → 文本（`2025-10` 形式）
   - 所有 `*_amt` / `*_cost` / `*_rate` / `*_qty` / `*_cnt` → **数值**

---

## 第 2 步：逐看板搭建

### 看板 1：经营总览大屏（数据源 `ads_boss_dashboard`）

> ⚠️ **这是重灾区，必须加日期筛选器**

**① 先加全局筛选器（最关键的一步）**
- 拖入「**统计日期**」字段 → 选择「**日期筛选器**」组件 → 放到大屏顶部
- 默认值设为「**最新日期**」
- 打开「**联动**」，让它影响本页所有组件

**② 6 个指标卡**（加了日期筛选器后，"求和"就等于当天值，安全）

| 卡片 | 字段 | 聚合 |
|---|---|---|
| 当日总营收 | total_revenue | 求和 |
| 当日总利润 | total_profit | 求和 |
| 利润率 | profit_rate | 平均值 |
| 产能达成率 | capacity_achieved | 平均值 |
| 订单履约率 | order_fulfill_rate | 平均值 |
| 库存周转天数 | inventory_turnover | 平均值 |

> 卡片标题建议把"当日"改成"**选定日**"，避免和筛选器语义打架。
> 如果不想加筛选器，就全部改成「平均值」，但标题要改成"**日均**"。

**③ 营收/利润趋势**：折线图
- X 轴：统计日期｜Y 轴：`SUM(total_revenue)`、`SUM(total_profit)`

**④ 成本结构分析**：100% 堆叠柱状图
- X 轴：统计日期（按**月**分组）｜Y 轴：`AVG(material_cost_ratio)`、`AVG(labor_cost_ratio)`、`AVG(mfg_cost_ratio)`
- 这三列本身就是占比，**不要求和**

**⑤ 完成后对答案（选定 2026-06-29）**

| 指标 | 正确值 |
|---|---|
| 当日总营收 | **124,570,787.51** |
| 当日总利润 | **28,879,058.10** |
| 利润率 | **23.18%** |
| 产能达成率 | **77.58%** |
| 库存周转天数 | **20.39** |
| 订单履约率 | **88.06%** |
| 物料/人工/制造占比 | **59.99% / 22.00% / 18.01%** |

---

### 看板 2：销售分析（数据源 `ads_sale_analysis`）

**筛选器**：区域（下拉，7 个值）、统计日期（日期范围）

| 组件 | 类型 | 维度 | 指标/聚合 |
|---|---|---|---|
| 销售趋势 | 折线图 | stat_date | `SUM(order_amt)` |
| 客户排行 Top10 | 横向条形图 | customer_name | `SUM(order_amt)`，排序降序 + TOP 10 |
| 产品排行 Top10 | 横向条形图 | product_name | `SUM(order_amt)`，排序降序 + TOP 10 |
| 区域分布 | 饼图（或地图） | region | `SUM(order_amt)` |
| 交付及时率 | 指标卡 | — | 计算字段 `SUM(order_cnt*delivery_ontime_rate)/SUM(order_cnt)` |

**销售明细表**
- 列：统计日期、客户名称、产品名称、区域、订单数、订单金额、回款金额
- ⚠️ **排序：统计日期 降序**（原看板首屏全是 2025-10-01，就是因为没排序）
- 建议只显示前 500~1000 行，避免拖动卡顿

---

### 看板 3：生产监控（数据源 `ads_produce_monitor`）

**筛选器**：车间（下拉，6 个值）、统计日期（日期范围）

**新建 2 个计算字段**（这是本表的核心）：
- `产能达成率_正确` = `SUM(actual_qty) / SUM(plan_qty)`
- `良品率_正确` = `SUM(qualified_qty) / SUM(actual_qty)`

| 组件 | 类型 | 维度 | 指标 |
|---|---|---|---|
| 当前产能达成率 | 指标卡 | — | `产能达成率_正确` |
| 良品率变化趋势 | 折线图 | stat_date | `良品率_正确` |
| 不良类型分布 | 饼图 | defect_type | `SUM(defect_qty)` |
| 车间产能/良率对比 | 双柱状图 | workshop_name | `产能达成率_正确`、`良品率_正确` |

**生产工单明细表**
- 列：统计日期、车间名称、产品名称、计划产量、实际产量、良品数量、不良品数量、主要不良类型、产能达成率
- ⚠️ **排序：统计日期 降序**

> 对答案：产能达成率应显示 **77% 左右**（不是 1976 万）；良品率趋势应在 **90%~91%** 区间。

---

### 看板 4：库存健康（数据源 `ads_stock_health`）

> 本表是**单日快照**（当前 600 行 @ 2026-07-31），所以**不需要**日期筛选器就能算对；
> 但建议仍加一个「统计日期」筛选器，将来快照积累多天后不会出错。

| 组件 | 类型 | 维度 | 指标 |
|---|---|---|---|
| 库存金额 | 指标卡 | — | `SUM(stock_amt)` |
| 库存数量 | 指标卡 | — | `SUM(stock_qty)` |
| 呆滞物料占比 | 指标卡 | — | 计算字段 `SUM(is_slow_moving)/COUNT(*)*100` |
| 库存金额排行 | 横向条形图 | material_name | `SUM(stock_amt)`，TOP 20 |
| 仓库库存结构 | 饼图 | warehouse_id | `SUM(stock_amt)` |
| 周转天数分布 | 柱状图 | turnover_days（分箱：0-7/7-15/15-30/30+） | `COUNT(*)` |

**库存明细表**
- 列：物料名称、仓库ID、库存数量、库存金额、周转天数、是否呆滞
- **条件格式**：`is_slow_moving = 1` 的行标红 / 加"呆滞"标签
- 排序：库存金额 降序

> 对答案：**600 行**、库存金额 **98,923.9 万元**、呆滞 **12 个（2.00%）**。

---

### 看板 5：成本利润分析（数据源 `ads_cost_profit`）

**筛选器**：统计月份（下拉，9 个值：2025-10 ~ 2026-06）、车间

| 组件 | 类型 | 维度 | 指标 |
|---|---|---|---|
| 平均毛利率 | 指标卡 | — | `AVG(profit_rate)` |
| 总成本 | 指标卡 | — | `SUM(total_cost)` |
| 月度毛利率趋势 | 折线图 | stat_month | `AVG(profit_rate)` |
| 成本结构趋势 | 堆叠柱状图 | stat_month | `SUM(material_cost)`、`SUM(labor_cost)`、`SUM(mfg_cost)` |
| 产品毛利率排行 | 横向条形图 | product_name | `AVG(profit_rate)`，降序 |
| 产品单位成本排行 | 横向条形图 | product_name | `AVG(unit_cost)`，降序 |

**成本利润明细表**
- 列：统计月份、产品名称、车间名称、单位成本、销售均价、单位毛利、毛利率
- ⚠️ **排序：统计月份 降序**

> 对答案：**10,854 行**、平均毛利率 **22.93%**（区间 -125.17% ~ 34.38%）。
> 负毛利率是数据本身的分布（个别产品销售均价低于成本），如果想突出异常可以加条件格式标红。

---

### 看板 6：异常预警清单（数据源 `ads_alert_warning`）

**筛选器**：预警类型（5 个值）、预警等级（高/中/低）

| 组件 | 类型 | 维度 | 指标 |
|---|---|---|---|
| 预警总数 | 指标卡 | — | `COUNT(*)` |
| 高等级预警数 | 指标卡 | — | 计算字段 `SUM(IF(alert_level='高',1,0))` |
| 按类型分布 | 柱状图 | alert_type | `COUNT(*)` |
| 按等级分布 | 饼图 | alert_level | `COUNT(*)` |

**预警明细表**
- 列：预警日期、预警类型、预警等级、预警对象名称、当前值、阈值、预警描述
- **排序优先级**：预警等级（高→中→低）
- **条件格式**：高=红、中=橙、低=黄

> 对答案：**61 条**，5 类齐全（营收缺口/产能不足/库存积压/不良超标/回款滞后），等级有高有低。

---

### 看板 7：生产四象限（数据源 `ads_produce_monitor`）

> 把「车间产能达成率 × 良品率」画成四象限散点，一眼定位改进优先级。
> 阈值：产能达成率 **80%**、良品率 **95%**（与 notebook《02_capacity_quality_analysis》一致）。

**① 先建 2 个计算字段**
- `产能达成率_正确` = `SUM(actual_qty) / SUM(plan_qty) * 100`
- `良品率_正确` = `SUM(qualified_qty) / SUM(actual_qty) * 100`

**② 散点图（核心）**
- X 轴：`产能达成率_正确`｜Y 轴：`良品率_正确`｜颜色：车间 `workshop_name`
- 加两条参考线：X = 80、Y = 95，把图切成四个象限

| 象限 | 判定 | 建议动作 |
|---|---|---|
| 双优 | 产能≥80% 且 良品率≥95% | 标杆，输出经验 |
| 高产低质 | 产能≥80% 但 良品率<95% | 优先改善质量 |
| 低产高质 | 产能<80% 但 良品率≥95% | 优先提升产能 |
| 双低 | 产能<80% 且 良品率<95% | 产能质量双线整改 |

**③ 车间明细表（可选）**
- 列：车间名称、产能达成率、良品率、距 80%/95% 标准的差距
- 排序：改善优先级（缺口合计大的在前）

---

## 第 3 步：整体优化（可选但推荐）

1. **统一主题**：深色大屏用于"经营总览"，浅色用于分析类看板
2. **数字格式**：金额用「千分位 + 万/亿缩写」，比率保留 2 位小数
3. **联动**：大屏的日期筛选器 → 联动全页；分析看板的"区域/车间"筛选器 → 联动本页图表
4. **下钻**：区域分布图 → 点击区域下钻到该区域的客户明细
5. **移动端**：如果需要，把大屏布局重排为竖版

---

## 第 4 步：数据更新（抽取模式必看）

抽取模式下，**刷新浏览器不会重新取数**，必须：

> 「数据」→ 找到 `制造数仓看板` 主题 → 点「**更新**」→ 等提示完成 → 回看板 `Ctrl + F5`

建议在主题上配置「**定时更新**」（例如每天 08:30，ETL 完成之后），就不用每次手点了。

---

## 第 5 步：验收清单（逐项打勾）

| # | 检查项 | 期望 |
|---|---|---|
| 1 | 大屏任意 KPI 卡不出现 8 位以上数字 | 营收应在千万级（1.2 亿），不是 226 亿 |
| 2 | 大屏所有 % 类指标都在 0~100 之间 | 利润率 23.35、产能 77.47、履约率 88.71 |
| 3 | 库存健康明细表有 600 条、金额非 0 | 92,755.3 万元 |
| 4 | 成本利润明细有 10,854 条、覆盖 9 个月 | 2025-10 ~ 2026-06 |
| 5 | 生产监控不良类型有 4 类 | 尺寸偏差/外观缺陷/性能失效/其他 |
| 6 | 异常预警有 5 种类型 | 且等级含高/中/低 |
| 7 | 所有明细表首行是最新日期 | 不是 2025-10-01 |
| 8 | 图表 Y 轴量级合理 | 良品率 90 左右，不是 80,000 |

---

## 附：常踩的 6 个坑

1. **比率字段用了"求和"** ← 本次翻车的根本原因。任何 `%`、`天数` 字段，先问自己"加起来有业务意义吗"
2. **快照表没有日期筛选器** ← `ads_boss_dashboard` 有 272 行，不加筛选器就是累计值
3. **明细表没排序** ← 首屏永远显示最老的数据，看着像"没更新"
4. **抽取模式忘了点更新** ← 数据改了看板不变
5. **TOP N 忘了设排序** ← "客户排行"会把客户按名字排而不是按金额排
6. **计算字段漏了分母保护** ← 建议写成 `SUM(a)/NULLIF(SUM(b),0)`，避免除零显示异常

---

## 附：数据现状速查（2026-09-16）

| 表 | 行数 | 时间范围 |
|---|---|---|
| ads_boss_dashboard | 272 | 2025-10-01 ~ 2026-06-29（逐日） |
| ads_sale_analysis | 852,421 | 2025-10-01 ~ 2026-06-29 |
| ads_produce_monitor | 255,943 | 2025-10-01 ~ 2026-06-29 |
| ads_stock_health | 600 | 2026-07-31（单日快照） |
| ads_cost_profit | 10,854 | 2025-10 ~ 2026-06（9 个月） |
| ads_alert_warning | 61 | 五类预警（含营收缺口） |

---
---

# 实操篇：从零搭第一个看板（点哪里）

> 基于 FineBI 6.0 官方流程：**新建分析主题 → 数据 → 组件 → 仪表板**
> 参考：[分析主题概述](https://help.fanruan.com/finebi6.X/doc-view-1888.html)、[制作第一个组件](https://help.fanruan.com/finebi6.0/doc-view-1653.html)

## A. 先理解 3 个概念，后面全是套路

| 概念 | 是什么 | 类比 |
|---|---|---|
| **分析主题** | 装一切的容器，有「数据 / 组件 / 仪表板 / 分析文档」4 个 TAB | 一个项目文件夹 |
| **组件** | 一张图表（指标卡、折线、饼图、明细表…） | 一块积木 |
| **仪表板** | 把多块积木拼成一页 | 拼好的成品 |

**每个组件都只由两部分组成**：

- **维度** = 按什么分类（日期、客户、产品、车间、区域…）
- **指标** = 算什么数（金额、数量、比率…）→ **指标必须选聚合方式**

> ⚠️ **上一版翻车就翻在这里**：`ads_boss_dashboard` 有 272 行（一天一行），
> 指标卡默认"求和"就把 272 天加起来了。**聚合方式必须和表的粒度匹配**（见手册开头的铁律表）。

## B. 五分钟做出第一个组件（指标卡）

1. 「我的分析」→ 选个文件夹 →「**新建分析主题**」，命名 `制造数仓看板`
2. 切到「**数据**」TAB → 点「**添加**」→「数据库表」→ 连接选「**数据连接**」→ 库选 `ads_db` → 勾选 `ads_boss_dashboard` → 确定
3. 切到「**组件**」TAB →「**新建组件**」→ 选择数据集 `ads_boss_dashboard`
4. 左侧图表类型选「**指标卡**」
5. 把字段 `total_revenue` 拖到「**指标**」区
6. 点这个指标 → 聚合方式选「**求和**」
7. 右上角「保存」→ 命名"当日总营收"
8. 重复 5~7 做出另外 5 个指标卡（利润率/产能达成率/订单履约率/库存周转天数/当日总利润）
   - **比率类**（利润率、产能达成率、订单履约率、库存周转天数）聚合方式选「**平均值**」

## C. 搭完「经营总览大屏」

**① 先加日期筛选器（最重要的一步）**
1. 切到「**仪表板**」TAB →「**新建仪表板**」
2. 上方「添加组件」→「**过滤组件**」→「**日期过滤**」
3. 绑定字段选 `stat_date`
4. 拖到仪表板**顶部**，右边的设置里把默认值设为「**最新日期**」
5. 开启「**联动**」，让它影响本页所有组件

**② 把 6 个指标卡拖进仪表板**
- 从右侧"组件"列表把它们依次拖入，排成一行
- 拖完检查：营收应该显示**千万级**（1.2 亿左右），不是 226 亿

**③ 加营收/利润趋势（折线图）**
- 新建组件 → 数据集 `ads_boss_dashboard` → 图表类型「**折线图**」
- 维度：`stat_date`｜指标：`total_revenue`、`total_profit`（都是求和）
- 拖进仪表板，放在指标卡下面

**④ 加成本结构分析（100% 堆叠柱）**
- 新建组件 → 图表类型「**堆积柱状图**」
- 维度：`stat_date`（点字段旁的设置，改成按**月**显示）
- 指标：`material_cost_ratio`、`labor_cost_ratio`、`mfg_cost_ratio` —— 全部选「**平均值**」

**⑤ 保存主题**（直连模式不需要"更新"）

## D. 其余 5 个看板的套路

全部是同一个动作：**新建组件 → 拖维度 → 拖指标 → 选聚合 → 保存 → 拖进仪表板**。
差别只在"哪些字段、什么聚合、什么图"——手册前面的逐看板清单已经列全了。

**做的时候记住 4 条**：

| 表格类型 | 指标聚合 |
|---|---|
| 明细表（销售分析/生产监控/成本利润/库存明细） | 金额、数量 → **求和**；比率 → **平均值** |
| 时间趋势图 | 数值 → 求和；比率 → 平均值 |
| 排行图（Top10） | 求和 + **排序降序 + TOP N** |
| 分布图（饼图） | 求和（占比会自动算） |

## E. 5 个必须设对的地方

**① 计算字段**（比率算不准时用）
- 在生产监控的组件编辑页，左侧字段列表右上角「**新建计算字段**」
- 名称 `产能达成率_正确`，公式：`SUM(actual_qty) / SUM(plan_qty)`
- 名称 `良品率_正确`，公式：`SUM(qualified_qty) / SUM(actual_qty)`
- 库存健康再做 `呆滞占比`：`SUM(is_slow_moving) / COUNT(*)*100`

**② 排序**
- 排行图：把指标字段拖到「**排序**」区，选**降序**
- 明细表：把 `stat_date` 或 `stat_month` 拖到排序区选**降序** ←（不设的话首屏永远是 2025-10-01）

**③ TOP N**
- 排行图右侧设置里找「**结果过滤器**」/「显示前 N 项」→ 填 10 或 20

**④ 数字格式**
- 点指标字段 → 「**数值格式**」：金额勾选"千分位"；比率选"百分比"、保留 2 位小数

**⑤ 条件格式**（预警清单必用）
- 明细表 → 把 `alert_level` 拖到「**颜色**」区 → 手动分配：高=红、中=橙、低=黄

## F. 布局与美化

1. **筛选器统一放页面顶部**，所有图表都联动它
2. **大屏用深色主题，分析类看板用浅色**（仪表板右上角「主题」里换）
3. **卡片大小对齐**：拖的时候注意对齐辅助线；选中多个组件可以「等宽/等高」
4. **一屏原则**：经营总览大屏尽量不出现滚动条
5. **每个组件标题写清口径**，例如"利润率（选定日）"，避免和"日均"混淆

## G. 搭完自检 4 条

1. 大屏点开时，**营收卡在千万级、所有百分比都在 0~100 之间**
2. 随便选一个别的日期，**所有卡片跟着变**（说明筛选器联动生效）
3. 明细表第一行是**最新日期**
4. 排行图是从大到小排的

> 如果第 1 条不满足（比如营收还是 226 亿），说明那张卡没被日期筛选器覆盖到，
> 去检查筛选器的「联动」设置，或把该卡片的聚合方式改成「平均值」。

