# BI 看板数据修正说明（2026-09-10）

> 结论先行：你感觉"数据不对"是真的。问题不在 FineBI，而在 `step04` / `step05` 两个 ETL 脚本——
> 里面写死了 6 处假指标、库存链路断在 DWD 层、时间范围写死、营收口径把"已取消订单"也算进去了。
> 本次已全部修好并重跑，**6 张 ADS 表的字段名/字段数一张都没变**，所以 FineBI 里已做的图表绑定不用重做，刷新即可。

---

## 第四轮（2026-09-16）：收尾打磨 + 全量重建（当前库内真实数字以此为准）

> 本轮完成了工程化收尾（git 版本控制、垃圾清理、notebook 连接改环境变量），
> 并对代码/数据做了一轮打磨后**全量重建源数据 + 重跑 ETL**。
> 下面第三节的"第一轮数字"（273/695618/200/74 等）是中间态，已被本轮覆盖，**以本节为准**。

### 本轮改动
- 生成器补「已退货」状态 → `return_amt`（退货金额）不再恒为 0（现约 **5.6 亿元**）。
- `dim_date` 从"写死日期 + 无人使用"改为"按 ODS 实际日期**动态重建**"，并接入大屏日期脊柱。
- 新增 `sql/00_init_all.sql`（建 7 库 + 全部建表，一键初始化）。
- 「回款滞后」预警去 `CURDATE()`，改按数据内最新发货日（幂等，重跑结果不变）。
- 「营收缺口」预警阈值 15% → 12%（基础取消率约 10%，避免空预警）。
- 重写 `cleanup-c-drive.ps1`；删 `sql/_backup_20260910/`（有 git 后冗余）。

### 当前数据现状（2026-09-16 实测）

| 表 | 行数 | 日期范围 |
|---|---|---|
| ads_boss_dashboard | 272 | 2025-10-01 ~ 2026-06-29（逐日） |
| ads_sale_analysis | 852,421 | 2025-10-01 ~ 2026-06-29 |
| ads_produce_monitor | 255,943 | 2025-10-01 ~ 2026-06-29 |
| ads_stock_health | 600 | 2026-07-31（基准日） |
| ads_cost_profit | 10,854 | 2025-10 ~ 2026-06（9 个月） |
| ads_alert_warning | 61 | 五类预警 |

关键指标（最新日 2026-06-29）：当日总营收 **1.07 亿**、利润率 **23.35%**、产能达成率 **77.47%**、
订单履约率 **88.71%**、库存周转 **19.96 天**、成本结构 物料/人工/制造 = **59.98% / 22.01% / 18.01%**。

全量口径：营收 **208.08 亿**（DWD=DWS=ADS）；生产 plan/actual/defect = **54,759,726 / 42,384,170 / 3,742,776**。

---

## 一、本次改动清单

| 类型 | 文件 | 说明 |
|---|---|---|
| 新增 | `sql/03_dwd_db/dwd_stock_snapshot.sql` | DWD 库存快照事实表（原项目缺这张表，是库存看板全为 0 的根因） |
| 修改 | `sql/06_etl_scripts/step03_dwd_fact.sql` | 增加"库存快照事实表"入仓（第 11 段） |
| 修改 | `sql/06_etl_scripts/step04_dws_agg.sql` | 库存日汇总改取真实快照、营收剔除已取消、成本环比差异率真实计算 |
| 修改 | `sql/06_etl_scripts/step05_ads_calc.sql` | 全部去硬编码、动态时间范围、大屏逐日快照、五类预警、真实毛利 |
| 修改 | `sql/03_dwd_db/dwd_stock_io_detail.sql` | 新增复合索引 `idx_mat_wh_date(material_id, warehouse_id, io_date)`（已同步 ALTER 线上表） |
| 备份 | `sql/_backup_20260910/` | 三个被改脚本改前的原样备份（项目没有 git，先备后改） |

重跑耗时（本地）：step03 约 222s、step04 约 290s、step05 约 400s，合计约 15 分钟，远低于说明书 2 小时上限。

---

## 二、原来的 8 个问题（含证据）与修复结果

### 1. 库存看板整块是空的（最严重）

- **面板上**："共81条"，库存数量 `0`、库存金额 `0`、周转天数 `45`、是否呆滞 `0`，库存金额排行全 0。
- **原因**：`step04` 原第 59-60 行直接 `0 AS stock_qty, 0 AS stock_amt`，注释写"使用 dwd_stock_io_detail 替代缺失的 dwd_stock_snapshot"——即 DWD 层没建库存快照表。
- **实际上数据是有的**：`wms_db.stock_snapshot` / `ods_db.ods_wms_stock_snapshot` 共 66,800 行，覆盖 2025-09-01 ~ 2026-07-31，每天 200 条，3 个仓库齐全。
- **修复**：新建 `dwd_stock_snapshot`，`dws_stock_day` 的库存数量/金额改取快照结存，周转天数 = 当日库存金额 ÷ 近 30 天日均出库金额。
- **结果**：200 行真实库存，库存金额 **4,709.6 万元**，平均周转 **1.23 天**，呆滞 6 个（**3.00%**）。

### 2. 六处硬编码的"假指标"

| 位置 | 原来（写死） | 现在（真实计算） |
|---|---|---|
| 大屏 利润率 | `15.00` | 由"当月已售产品成本率"反算，抽样日 90.56% |
| 大屏 库存周转天数 | `45.00` | 公司级 **0.45 天** / 物料级平均 1.23 天 |
| 大屏 成本结构 | `45 / 25 / 30`（恒等于 100%） | 2026-06 实际：物料 **58.84%**、人工 **23.55%**、制造费用 **17.61%** |
| 大屏 当日总利润 | `营收 × 0.15` | 营收 − 当月单位成本 × 销量（真实毛利） |
| 成本分析 销售均价 | `单位成本 × 1.3` | 当月营收 ÷ 销量（真实售价） |
| 成本分析 毛利率 | 恒 `23.08%` | 真实区间 **24.92% ~ 95.18%** |
| 生产 不良类型 | 恒 `'尺寸偏差'` | 源表真实 4 类：其他/尺寸偏差/性能失效/外观缺陷 |
| 预警 等级 | 恒 `'高'` | 按偏离度分 高/中/低 |
| 库存周转天数 | 恒 `45` | 真实计算 |
| 成本差异率 | 恒 `0` | 按"同产品同车间"逐月环比计算 |

### 3. 大屏"当日总营收"其实是 9 个月累计，且只有 1 行

- 面板显示"当日总营收 1,287,661,602.17"，实际是 `2025-10-01~2026-06-30` 九个月累计；`ads_boss_dashboard` 表**只有 1 行**（`stat_date = CURDATE()`）。
- 后果：趋势、同比、环比、日期筛选统统做不了。
- **修复**：改成**逐日快照**，现 **273 行（2025-10-01 ~ 2026-09-09）**，每天一行真实指标。

### 4. 营收虚高约 20%：已取消订单被计入了营收

- 说明书 7.1 明确"营业收入 = 当期有效订单含税总金额（**剔除取消、作废、纯退款订单**）"，但 `step03` 只过滤了"已作废"，`step04` 未剔除"已取消"。
- **已取消订单金额 2.71 亿元**（142,524 单）被算进了营收。
- **修复**：`dws_sale_day` / `dws_sale_month` 增加 `order_status NOT IN ('已取消','已作废')`。
- **结果**：营收口径由 **16.26 亿 → 13.55 亿**（这是本次唯一会"数字变小"的改动，属口径纠正）。

### 5. 时间范围写死，看板少 3 个月数据

- 所有 ADS 都写死 `BETWEEN '2025-10-01' AND '2026-06-30'`，而库里数据已到 2026-09-09，成本分析更是写死 `>= CURDATE() - INTERVAL 3 MONTH` 导致只剩 1 个月。
- **修复**：全部改为按库内数据动态计算。成本利润分析从 **1 个月 1,206 行 → 9 个月 10,854 行**。

### 6. 口径名不符实：`oee_rate` 其实是产能达成率

- `dws_produce_day.oee_rate` 的计算是 `实际产量/计划产量`，这是**产能达成率**，不是 OEE（OEE 需用设备运行记录算时间开动率×性能开动率×合格品率）。
- 大屏却把它当"产能达成率"用，属于"算对了但名字错了"。
- **修复**：保留列名（避免影响绑定），修正注释说明；真正的 OEE 需要用 `dwd_equipment_runtime` 单独算，本次未做，需要的话可以补。

### 7. 预警只有 1 类，且榜单被"最近 100 条"污染

- 原来 100 条全是"产能不足 / 高 / 2026-09-01"，说明书 6.4 要求的 5 类（营收缺口、产能不足、库存积压、不良超标、回款滞后）一类都没有。
- **修复**：五类全部实现，共 **74 条**：回款滞后 30、营收缺口 30、库存积压 6、不良超标 4、产能不足 4。

### 8. 库存健康表结构缺陷

- 原实现 `ORDER BY stock_amt DESC LIMIT 100` 只留金额前 100 行，导致"呆滞物料占比"这个指标**在数学上永远算不准**。
- **修复**：去掉 LIMIT，全量入库（200 行），呆滞占比 3.00%。

---

## 三、修复后的数据校验（DWD = DWS = ADS）—— 第一轮中间态，已被第四轮全量重建覆盖

```
营收   DWD 13.54815423 亿 = DWS日汇总 = DWS月汇总 = ADS销售分析 = ADS大屏   ✓
生产   plan_qty   510,142,611 = 255,928 行 ADS 合计                        ✓
       actual_qty 396,752,980 = 255,928 行 ADS 合计                        ✓
       defect_qty  32,925,782 = 255,928 行 ADS 合计                        ✓
```

各表最终状态：

| 表 | 行数 | 日期范围 |
|---|---|---|
| ads_boss_dashboard | 273 | 2025-10-01 ~ 2026-09-09（逐日） |
| ads_sale_analysis | 695,618 | 2025-10-01 ~ 2026-09-09 |
| ads_produce_monitor | 255,928 | 2025-10-01 ~ 2026-09-09 |
| ads_stock_health | 200 | 2026-06-29（基准日） |
| ads_cost_profit | 10,854 | 2025-10 ~ 2026-06（9 个月） |
| ads_alert_warning | 74 | 五类预警 |

---

## 四、FineBI 6.0 侧要做的 3 件事

**你的数据连接已确认**：连接名"制造数仓"，`jdbc:mysql://localhost:3306/ads_db`（从 FineBI 仓库 `FINE_CONF_ENTITY` 中读到的），是**直连 MySQL**。

1. **刷新数据**：字段名和字段数完全没变，所以图表绑定不用动。
   - 若数据集是「直连数据」→ 浏览器里刷新看板即可。
   - 若数据集是「抽取数据」→ 进入「数据」→ 对应数据集 → 点「更新」，再刷新看板。
2. **大屏建议加"统计日期"筛选器**：`ads_boss_dashboard` 从 1 行变成 273 行，KPI 卡片如果设的是**求和**，会显示成累计值。
   - 指标卡建议：绑定 `stat_date = 最新日期`，或加日期筛选器（默认最新一天 / 或按月）。
   - 想做趋势的话，现在数据够了，可以直接拉"营收/利润率/产能达成率"按日期折线。
3. **库存看板注意基准日**：基准日为 **2026-06-29**（理由见下方第 3 条待办），页面标题建议标注"数据截止日"，避免和 7 月的快照混淆。

### 逐看板可优化点

- **经营总览大屏**：加日期筛选器 + 环比/同比；成本结构现在有真实占比，可换成 100% 堆叠柱；库存周转天数已真实（0.45 天），注意说明是"公司级整体口径"。
- **库存健康**：`是否呆滞` 现在有真实取值（6 个），建议做成筛选器 + 条件着色；"库存金额排行"已有数据可正常排序；建议补「库龄」维度（需源表支持）。
- **销售分析**：销售明细表建议默认按「统计日期」**倒序**（现在首屏全是 2025-10-01）；客户名称列建议加宽（"富士康工业互联"被截成"士康工业互联"）；区域分布各区域金额接近是数据本身特性。
- **生产监控**：不良类型分布现在有 4 根真实的柱子；良品率趋势现在能一直到 2026-06-30，注意 2026-09-09 是**孤立一天**（7、8 月无数据），建议趋势图加日期筛选把孤点排除。
- **成本利润分析**：现在 9 个月都有数据；注意"销售均价"是产品级口径（同产品在不同车间行里价格会重复）。
- **异常预警清单**：五类都有数据，建议按「预警等级」排序 + 按类型分页签。

---

## 五、需要你决策的 4 件事（均已解决：1→第二轮改交付及时率；2/3→第二轮改生成器；4→第四轮阈值改 12%）

### 1. 订单履约率口径偏低（20.77%）

现在是 `按时交付订单数 ÷ 全部有效订单数`，而库里有 **17 万单尚未发货**（`delivery_date IS NULL`）也被算作"不按时"，把指标压到了 20%。

- 选项 A：维持现状（口径 = 全部订单履约率）。
- 选项 B：改为 `按时交付订单数 ÷ 已交付订单数`（口径 = 交付及时率），数字会明显提高。
- 这属于业务口径确认，我没有擅自改，等你定。

### 2. 模拟数据本身的真实性（建议调生成器）

真实数据算下来：单位成本 **5.44 元/件**，而销售均价 **37.72 元/件** → 毛利率 80%+，制造业偏高。

更关键的是**量级不匹配**：
- 生产实际产量 **3.97 亿件**，销售订单数量只有 **4,309 万件** → 产量是销量的 **9.2 倍**；
- 于是"总成本 21.6 亿 > 总营收 13.55 亿"，看板之间会互相打脸（原来用写死的 23.08% 毛利率把这个矛盾盖住了）。

建议改 `scripts/generate_fake_data.py`：让产量与销量处在同一量级，或让单位成本占售价的 60%~80%。

### 3. 增量脚本有两个缺口（导致看板"缺最近的数据"）

`scripts/generate_incremental_data.py` 只生成 **销售订单、工单、出入库、设备**，**不生成库存快照，也不生成成本凭证**，所以：

- 库存快照停在 **2026-07-31**，出入库流水停在 **2026-06-29** → 库存看板基准日只能取 6-29（两边都齐的最后一天）；
- 成本凭证停在 **2026-06** → 成本/利润指标 7 月以后为**空值（NULL）**，我没有用估算值去填，宁可留空；
- 销售与生产数据在 **2026-07、2026-08 完全空白**，只有 2026-09-09 一天孤点 → 大屏 7、8 月无行。

建议给增量脚本补上"库存快照 + 成本凭证 + 连续日期"的生成逻辑；补上以后，库存基准日和大屏日期会自动前移，**SQL 不用再改**。

### 4. 「营收缺口」预警口径我换了，需要你确认

原口径（产品营收环比下降 ≥ 20%）在当前数据下**恒为空**——我实测了产品级、客户级、区域级三个层面，环比下滑数量都是 **0**（2026-06 所有产品都在涨，301 个客户一个没流失）。

所以改成了更有意义的：**「已取消订单金额」占当月下单金额 ≥ 15% 的产品**，预警描述写明缺口金额与取消率（如"六角螺母 2026-06 取消订单金额 290,642.79 元，占当月下单金额 21.55%"）。

- 如果你有**月度营收目标表**，更标准的做法是"实际营收 < 目标 × 80%"，我可以再改。

---

## 六、回滚方式

```powershell
Copy-Item D:\data_warehouse_project\sql\_backup_20260910\*.sql D:\data_warehouse_project\sql\06_etl_scripts\ -Force
```

回滚后重新执行 step03 → step04 → step05 即可恢复旧逻辑（`dwd_stock_snapshot` 表可留着，不影响旧脚本）。

---

## 七、两个运维提醒

1. **`TRUNCATE` 会隐式提交事务**，而调度器是"整个脚本成功才 commit"。结果是：如果 step04 跑到一半失败，**前面的 TRUNCATE 已经生效、后面的 INSERT 被回滚 → 表被清空**。（我这次改脚本时踩到过一次，已重跑修好。）建议后续改成"写入临时表 → 成功后再原子切换"，或把 TRUNCATE 挪到 INSERT 之后。
2. **本次执行环境说明**：为了核对 PDF 面板上的数字，我在你的 Python 里装了 `pymupdf`（渲染 PDF）和 `rapidocr-onnxruntime`（中文 OCR），过程产物在 `_bi_review\` 目录（面板截图、OCR 文本），**确认无误后可整个删除**，不影响项目。

---

# 第二轮修改（2026-09-10 晚）

## 一、订单履约率口径已按你的要求改掉

- 口径：**按时交付订单数 ÷ 已交付订单数**（未发货订单不再计入分母）。
- 改动：`dws_sale_day` 新增 `delivery_cnt`（已交付订单数）字段，`step04`/`step05` 同步改公式。
- 已重跑验证：全部有效订单 710,698 条、已交付 568,748 条，**履约率 20.77% → 26.73%**。

> ⚠️ 为什么只涨到 26.73%：当前库里的数据是**旧生成器**造的，发货日期是「下单后 0~30 天均匀随机」，
> 与"7 天按时"这个口径无关，按时率天然只有 7/30 ≈ 23%。新生成器已把按时率做成 88%，
> **重跑数据后该指标会升到 85%~90%**。

## 二、generate_fake_data.py：价格 / 成本 / 产量已自洽

新增一套**按产品 ID 确定性生成**的价格成本口径（同一产品每次运行结果一致）：

| 项目 | 公式 | 实测（2000 个产品抽样） |
|---|---|---|
| 产品单位成本 | 80 ~ 600 元/件 | 平均 341.75 |
| 产品标准售价 | 单位成本 ÷ (1 − 目标毛利率) | 平均 475.34 |
| 目标毛利率 | 22% ~ 34% | 实际 22.00% ~ 34.00%，平均 **27.89%** |
| 物料单价 | 5 ~ 800 元/件 | 平均 401.77 |
| 产量 / 销量 | 工单计划产量区间收窄为 20~200 | **1.09 倍**（原来 9.2 倍） |

同时修掉的问题：

- **成本与产量脱钩**：原来每张凭证成本是 `uniform(100,5000)` 随机数，与产量无关 → 现在 `成本 = 单位成本 × 工单实际产量 ×(1±4%)`，按 **物料 60% / 人工 22% / 制造费用 18%** 拆分。
- **成本月份随机**：原来 `cost_month` 随机取 → 现在取工单的计划开工月份。
- **库存快照与出入库无关**：原来快照 `stock_qty` 每天独立 `random(0,2000)` → 现在**结存 = 期初 + 累计入库 − 累计出库**，出库量不超过当前结存（库存永不为负），覆盖 **200 物料 × 3 仓库 = 600 条/天**。
- **良品 + 不良 ≠ 实际产量**：原来先算良品/不良、再改实际产量（脏数据分支）→ 现在先定产量再拆分。
- **N+1 查询**：原来每生成一条工单就 `SELECT workshop_name`，50 万次单行查询是整脚本跑 8 小时的**主因** → 现在一次 load 成字典。
- **设备运行四项时间之和 ≠ 1440**：OEE 无法计算 → 现在 `运行 + 空闲 + 故障 + 维护 = 1440`。
- **重复运行成倍累加**：脚本原来不清表（库里 100 万订单其实是多次累加的结果）→ 现在默认**先 TRUNCATE 11 张源表**，加 `--append` 可关闭。

## 三、generate_incremental_data.py：补齐了两个缺口

| 问题 | 原来 | 现在 |
|---|---|---|
| 不生成成本凭证 | 成本/利润看板永远停在初始区间 | 新增 `generate_incremental_cost_voucher()`，与工单产量、产品单位成本挂钩 |
| 不生成库存快照 | 库存看板永远停在初始区间 | 新增 `generate_incremental_stock()`，读上一日快照做期初、按当天出入库滚动结存 |
| 日均量级不匹配 | 写死 50 单/天，历史日均 3,663 单 → 增量日营收只有历史的 1/70 | 启动时**从历史自动推算日均量** |
| 密码占位符 | `'你的密码'`，一跑就连接失败 | 支持 `DB_PASSWORD` 环境变量，默认 `root` |
| 日期写死"今天" | 7、8 月缺口补不上 | 支持命令行区间回补，**已有数据的日期自动跳过**，不会重复累加 |
| 出入库不挂单据 | `workorder_id`/`order_id` 恒为空 | 出库单挂当天真实工单/订单 |

## 四、FineBI 刷新后数据没变——原因已查明

**你的分析主题用的是「抽取数据」而不是直连**，所以刷新浏览器不会重新取数。而且我查到：

- MySQL 语句统计显示，**20:43~20:46 有一次完整的抽取更新**，把 6 张 ads 表全拉了一遍（`SELECT * FROM ads_sale_analysis / ads_produce_monitor / ads_boss_dashboard / ads_alert_warning / ads_stock_health / ads_cost_profit`）；
- 抽取目录 `bi-data\spider\db` 在 20:44:32~20:45:13 被重写；
- FineBI 日志：`recordFinishCount BASE:93 / recordSuccessCount BASE:92`、`ETL:72 / 71`，**更新是成功的**。

**所以数据其实已经进抽取了。** 如果页面上还是老数字，按顺序排查：

1. **强制刷新浏览器**：`Ctrl + F5`，或关掉标签页重新打开看板。
2. **对着这几个"一眼可辨"的数字核对**，对上了说明新数据已生效：

   | 位置 | 老数据 | 新数据 |
   |---|---|---|
   | 库存健康 · 库存金额 | 0 | **4,709.6 万元**，200 行，基准日 2026-06-29 |
   | 库存健康 · 周转天数 | 45 | **1.23 天** |
   | 经营总览 · 库存周转天数 | 45 | **0.45 天** |
   | 成本利润分析 · 月份数 | 1 个 | **9 个（2025-10 ~ 2026-06）** |
   | 异常预警清单 · 预警类型 | 1 类 | **5 类** |
   | 生产监控 · 不良类型 | 1 类"尺寸偏差" | **4 类** |

3. **注意 KPI 卡的聚合方式**（很可能就是你觉得"还是不对"的原因）：`ads_boss_dashboard` 从 **1 行变成 273 行逐日快照**。卡片若用「求和」，"当日总营收"会显示 **13.55 亿**（273 天累计）。正确做法：指标卡绑定 `stat_date = 最新日期`，或加「统计日期」筛选器（默认最新一天）。
4. 若以上都对不上，回到「数据」里对**该分析主题**单独点一次「更新」（不要只刷新页面）。

## 五、你自己跑数据的命令

```powershell
cd D:\data_warehouse_project

# 1) 全量重建源数据（会先清空 erp_db/mes_db/wms_db 的 11 张表；约 40~90 分钟）
python scripts\generate_fake_data.py

# 2) 跑 ETL 全流程（约 20 分钟：step01~step05）
python scripts\etl_scheduler.py --once

# 3) 之后每天补增量（默认只生成"今天"）
python scripts\generate_incremental_data.py
#    或补齐区间缺口（已有数据的日期自动跳过）
python scripts\generate_incremental_data.py 2026-07-01 2026-09-10

# 4) FineBI：数据 → 该分析主题 → 更新 → 浏览器 Ctrl+F5
```

> 注意：**只跑生成器不跑 ETL，看板不会变**；第 1 步会清空源表，请确认没有其他程序在读这三个库。

---

# 第三轮：ETL 报 "table is full" 故障处理（2026-09-10 22:xx）

## 故障现象

```
step03_dwd_fact 执行失败: 第 9/12 条语句失败:
(1114, "The table 'dwd_stock_io_detail' is full") | 语句: INSERT INTO dwd_db.dwd_stock_io_detail ...（耗时 1155.0 秒）
```

## 根因（两条，MySQL 错误日志铁证）

```
21:58:39 [Error] InnoDB: Write to file .\dwd_db\dwd_stock_io_detail.ibd failed ...
                Check also that the disk is full
21:58:39 [Error] Server: The table 'dwd_stock_io_detail' is full!
```

### ① 磁盘被写满：binlog 与巨型事务

- MySQL 数据目录在 `C:\ProgramData\MySQL\...`，而 C 盘只有 200 GB、当时仅剩 12.85 GB；
- `log_bin = ON`，且 `binlog_expire_logs_seconds = 2592000`（保留 30 天）→ **binlog 堆积到 7,434 MB**，
  其中两个文件分别是 **1,120 MB / 971 MB**；
- `step03` 原来把**几百万行 INSERT 放在一个事务里**（调度器"整个脚本成功才 commit"），
  提交时一次性写出巨大的 binlog 事件 + 巨大的 undo，直接把盘顶爆。

### ② ODS 层数据翻倍（放大了写入量）

`step01` 是**追加式增量同步**（水位 + UPSERT），而源库被 `generate_fake_data.py` **全量重建**过
（主键全新、`update_time` 全是当天），于是水位（21:48:33）把这批数据全当成"新增"追加进 ODS：

| 表 | 源库 | ODS（故障时） |
|---|---|---|
| 销售订单 | 1,000,000 | 2,000,025 |
| 生产工单 | 500,000 | 1,000,015 |
| 成本凭证 | 500,000 | 1,000,000 |
| 出入库 | 492,340 | 992,360 |
| 客户 | 300 | 600 |

DWD 重建量因此翻倍 → 磁盘压力进一步放大。

### 附带发现：DWD 现在全空

`step03` 开头的 6 条 `TRUNCATE` 会**隐式提交**，而后面的 INSERT 随失败回滚 →
`dwd_sale_order_detail` / `dwd_stock_io_detail` / `dwd_produce_workorder_detail` /
`dwd_stock_dnapshot` / `dwd_cost_detail` 现在都是 **0 行**（表空间文件仍占 1.5 GB 未释放，重跑 TRUNCATE 时会自动回收）。

## 已完成的处置

| 动作 | 结果 |
|---|---|
| 清空 ODS 层 11 张表 + `etl_watermark` | ODS 归零，水位清空 → `step01` 下次自动全量同步 |
| `PURGE BINARY LOGS BEFORE NOW()` | binlog 7,434 MB → 971 MB，**C 盘 15.67 GB → 21.98 GB** |
| `SET GLOBAL binlog_expire_logs_seconds = 86400` | 保留期 30 天 → 1 天，不再堆积（重启后失效，需写进 my.ini） |
| 新增 `sql/06_etl_scripts/step00_ods_full_reset.sql` | "源库全量重建后必须清 ODS + 重置水位"的开关脚本（**不要放进每日调度链**） |
| `etl_scheduler.py` 新增 `COMMIT_PER_STATEMENT = True` | 每条 SQL 单独提交，峰值 undo/binlog 只有单条语句规模 |

## 重新运行的步骤

源数据**已经是新的、完整的**（1,000,000 订单 / 500,000 工单 / 500,000 成本凭证 / 492,340 出入库 / 200,400 库存快照），
ODS 已清空，所以**直接跑 ETL 即可**：

```powershell
cd D:\data_warehouse_project
python scripts\etl_scheduler.py --once      # 约 20~30 分钟
```

> **不需要重跑 `generate_fake_data.py`**。如果哪天真要再重新生成源数据，
> 顺序必须是：`generate_fake_data.py` → **`step00_ods_full_reset.sql`** → `etl_scheduler.py --once`。
> 少了 step00 这一步，ODS 就会再次翻倍。

## 其他建议（可选，进一步降低风险）

1. **把 MySQL 数据目录迁到 D 盘**（D 盘 751 GB、剩 230 GB）：停 MySQL → 复制 `Data` 目录 →
   改 `my.ini` 的 `datadir` → 重启。最彻底。
2. **关掉 binlog**（本项目没有主从复制，不需要 binlog）：`my.ini` 加 `skip-log-bin`，
   可显著降低磁盘占用并加快 ETL 写入。
3. **缩小数据量**：如把 `SALE_ORDER_COUNT` 从 100 万降到 30 万，全库体积约为现在 1/3。
