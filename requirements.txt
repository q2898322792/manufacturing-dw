# ============================================================
# requirements.txt —— Python 依赖清单
# 安装：pip install -r requirements.txt
# 版本号为建议下限，可按本机实际已装版本调整
# ============================================================

# ---- 数据生成 + ETL（scripts/）----
pymysql>=1.0.2          # 连接 MySQL
faker>=18.0             # 生成仿真数据（中文 locale）
schedule>=1.1.0         # ETL 定时调度（仅守护模式需要）

# ---- 分析 Notebook（notebooks/）----
pandas>=1.5.0           # 数据处理
numpy>=1.23.0           # 数值计算
matplotlib>=3.6.0       # 绘图
tabulate>=0.9.0         # DataFrame.to_markdown() 依赖
jupyter>=1.0.0          # 运行 .ipynb

# ---- 可选：_bi_review OCR 复核用（一般不需要装）----
# pymupdf>=1.24.0
# rapidocr-onnxruntime>=1.3.0
